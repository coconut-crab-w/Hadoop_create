### Chandy-Lamport 算法
- Flink 基于Chandy-Lamport算法实现了一个分布式的一致性的快照，从而提供了一致性的语义。

### Flink 的要点

- checkpoint
- State
- Time
- Window
- Watermark 的机制: 能够支持基于事件的时间的处理，或者说基于系统时间的处理，能够容忍数据的延时、容忍数据的迟到、容忍乱序的数据。
- 开窗：另外流计算中一般在对流数据进行操作之前都会先进行开窗，即基于一个什么样的窗口上做这个计算。Flink 提供了开箱即用的各种窗口，比如滑动窗口、滚动窗口、会话窗口以及非常灵活的自定义的窗口。

- Flink的核心计算引擎是“Flink Runtime 执行引擎”   ： Flink 分别提供了面向流式处理的接口（DataStream API）和面向批处理的接口（DataSet API）

- DataStream API 可以流畅地分析无限数据流，并且可以用 Java 或者 Scala 等来实现。开发人员需要基于一个叫 DataStream 的数据结构来开发，这个数据结构用于表示永不停止的分布式数据流。

- Flink 本质上使用容错性数据流，这使得开发人员可以分析持续生成且永远不结束的数据（即流处理）。



### Flink On Yarn 一个任务的启动流程

1. 当启动Flink-yarn的任务时，客户端会检查本次申请的资源（内存，计算）是否充足，如果充足，会上传包含HDFS以及Flink的配置信息和Flink的jar到HDFS上
2. 客户端向RM发出指令
3. RM 会指派NM 创建 Container，并从HDFS上下载jar以及配置文件
4. 启动 AM 和 jobmanager ，将jobmanager的地址信息写到配置文件中，在上传到HDFS上
5. 同时 AM 向 RM 发送心跳注册自己，其中包括申请的资源（cpu，内存）
6. 创建 TaskManager 容器，从 HDFS 中下载 jar 包及配置文件并启动；
7. 各 task 任务通过 jobmanager 汇报自己的状态和进度，AM 和 jobmanager 在一个容器上，AM 就能掌握各任务的运行状态，从而可以在任务失败时，重新启动任务；
8. 任务完成后，AM 向 RM 注销并关闭自己；


### 配置文件

- export HADOOP_CONF_DIR=/opt/module/hadoop-3.1.3/etc/hadoop


### cluster 模式启动命令

bin/flink run -m yarn-cluster -yn 2 -yjm 800 -ytm 800 /opt/moudle/examples/batch/WordCount.jar

- Flink 运行时需要配置 hadoop classpath ,执行下列命令进行动态获取
> export HADOOP_CLASSPATH=`hadoop classpath` 